const express = require(`express`);

const router = express.Router();

const { createAccount } = require(`../controllers/accountsControllers`);

router.post(`/accounts`, createAccount);
module.exports = router;
